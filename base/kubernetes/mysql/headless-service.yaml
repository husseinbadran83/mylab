apiVersion: v1
kind: Service
metadata:
  name: mysql
  annotations:
      argocd.argoproj.io/sync-wave: "-3"
  labels:
    app: mysql
spec:
  ports:
    - port: 3306
  clusterIP: None
  selector:
    app: mysql
